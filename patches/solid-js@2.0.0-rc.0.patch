diff --git a/dist/dev.cjs b/dist/dev.cjs
index eb15135bbd0f524729b336bdf3b14f5b97101a99..649920168cbd4d018d4714517d9fbd70c33eee4a 100644
--- a/dist/dev.cjs
+++ b/dist/dev.cjs
@@ -572,11 +572,21 @@ function hydratedCreateRenderEffect(compute, effectFn, options) {
 function hydratedCreateEffect(compute, effectFn, options) {
   return hydratedEffect(signals.createEffect, compute, effectFn, options);
 }
-function lazyHydrationLookup(comp, moduleUrl) {
+function lazyHydrationLookup(comp, moduleUrl, exportName) {
   const o = signals.getOwner();
   const key = o && o.id != null ? signals.peekNextChildId(o) : undefined;
   const cached = key != null ? globalThis._$HY?.modules?.[key] : undefined;
-  if (cached) return () => cached.default;
+  // Hydration resolves the component synchronously from the preloaded module
+  // — its default export, or the call-site `export` option (a literal present
+  // in both bundles, so the sync claim is preserved). A wrapper that selects
+  // an export at runtime inside the import thunk cannot work here: the thunk
+  // hasn't run, and rendering the raw namespace's default would silently
+  // orphan the server DOM (solidjs/solid#3011). Fail loudly in dev instead.
+  if (cached) {
+    const component = exportName ? cached[exportName] : cached.default;
+    if (typeof component !== "function") throw new Error(`lazy() (hydration id "${key}") preloaded a module whose "${exportName ?? "default"}" ` + "export is not a component. lazy() hydrates synchronously from the preloaded " + "module; select a named export with the { export } option, or re-export the " + "component as the module's default. Wrappers that pick an export at runtime " + "inside the import thunk are not supported.");
+    return () => component;
+  }
   if (!comp && moduleUrl) {
     throw new Error(`lazy() module "${moduleUrl}" (hydration id "${key}") was not preloaded before ` + "hydration. Ensure it is inside a Loading boundary.");
   }
@@ -984,30 +994,42 @@ function Hydration(props) {
 function createComponent(Comp, props) {
   return devComponent(Comp, props || {});
 }
-function lazy(fn, moduleUrl) {
+function lazy(fn, options, moduleUrl) {
+  // rc.0 transform compat: the current bundler pass injects moduleUrl as
+  // the second argument; solid@next moves it third behind the { export }
+  // options bag (56ca6470).
+  if (typeof options === "string") {
+    moduleUrl = options;
+    options = undefined;
+  }
+  const exportName = options && options.export;
   let comp;
   let p;
+  const load = () => {
+    if (p) return p;
+    const cur = p = fn();
+    cur.then(mod => {
+      comp = () => (exportName ? mod[exportName] : mod.default);
+    }, () => {
+      if (p === cur) p = undefined;
+    });
+    return cur;
+  };
   const wrap = props => {
-    if (sharedConfig.hydrating) comp = _lazyHydrationLookup(comp, moduleUrl);
+    if (sharedConfig.hydrating) comp = _lazyHydrationLookup(comp, moduleUrl, exportName);
     let local = comp;
     if (!local) {
-      p || (p = fn());
-      p.then(mod => {
-        comp = () => mod.default;
-      });
-      local = signals.createMemo(() => p.then(mod => mod.default));
+      load();
+      local = signals.createMemo(() => load().then(mod => exportName ? mod[exportName] : mod.default));
     }
     let Comp;
     return signals.createMemo(() => (Comp = (comp || local)()) ? signals.untrack(() => {
-      Object.assign(Comp, {
-        [$DEVCOMP]: true
-      });
       return Comp(props);
     }) : "", {
       sync: true
     });
   };
-  wrap.preload = () => p || ((p = fn()).then(mod => comp = () => mod.default), p);
+  wrap.preload = load;
   wrap.moduleUrl = moduleUrl;
   return wrap;
 }
diff --git a/dist/dev.js b/dist/dev.js
index e9b692addaef5c1285a9003fe4ff360e91d40d79..54265d2b07704d9613d187ab105f02e1a0472470 100644
--- a/dist/dev.js
+++ b/dist/dev.js
@@ -571,11 +571,21 @@ function hydratedCreateRenderEffect(compute, effectFn, options) {
 function hydratedCreateEffect(compute, effectFn, options) {
   return hydratedEffect(createEffect$1, compute, effectFn, options);
 }
-function lazyHydrationLookup(comp, moduleUrl) {
+function lazyHydrationLookup(comp, moduleUrl, exportName) {
   const o = getOwner();
   const key = o && o.id != null ? peekNextChildId(o) : undefined;
   const cached = key != null ? globalThis._$HY?.modules?.[key] : undefined;
-  if (cached) return () => cached.default;
+  // Hydration resolves the component synchronously from the preloaded module
+  // — its default export, or the call-site `export` option (a literal present
+  // in both bundles, so the sync claim is preserved). A wrapper that selects
+  // an export at runtime inside the import thunk cannot work here: the thunk
+  // hasn't run, and rendering the raw namespace's default would silently
+  // orphan the server DOM (solidjs/solid#3011). Fail loudly in dev instead.
+  if (cached) {
+    const component = exportName ? cached[exportName] : cached.default;
+    if (typeof component !== "function") throw new Error(`lazy() (hydration id "${key}") preloaded a module whose "${exportName ?? "default"}" ` + "export is not a component. lazy() hydrates synchronously from the preloaded " + "module; select a named export with the { export } option, or re-export the " + "component as the module's default. Wrappers that pick an export at runtime " + "inside the import thunk are not supported.");
+    return () => component;
+  }
   if (!comp && moduleUrl) {
     throw new Error(`lazy() module "${moduleUrl}" (hydration id "${key}") was not preloaded before ` + "hydration. Ensure it is inside a Loading boundary.");
   }
@@ -983,30 +993,42 @@ function Hydration(props) {
 function createComponent(Comp, props) {
   return devComponent(Comp, props || {});
 }
-function lazy(fn, moduleUrl) {
+function lazy(fn, options, moduleUrl) {
+  // rc.0 transform compat: the current bundler pass injects moduleUrl as
+  // the second argument; solid@next moves it third behind the { export }
+  // options bag (56ca6470).
+  if (typeof options === "string") {
+    moduleUrl = options;
+    options = undefined;
+  }
+  const exportName = options && options.export;
   let comp;
   let p;
+  const load = () => {
+    if (p) return p;
+    const cur = p = fn();
+    cur.then(mod => {
+      comp = () => (exportName ? mod[exportName] : mod.default);
+    }, () => {
+      if (p === cur) p = undefined;
+    });
+    return cur;
+  };
   const wrap = props => {
-    if (sharedConfig.hydrating) comp = _lazyHydrationLookup(comp, moduleUrl);
+    if (sharedConfig.hydrating) comp = _lazyHydrationLookup(comp, moduleUrl, exportName);
     let local = comp;
     if (!local) {
-      p || (p = fn());
-      p.then(mod => {
-        comp = () => mod.default;
-      });
-      local = createMemo$1(() => p.then(mod => mod.default));
+      load();
+      local = createMemo$1(() => load().then(mod => exportName ? mod[exportName] : mod.default));
     }
     let Comp;
     return createMemo$1(() => (Comp = (comp || local)()) ? untrack(() => {
-      Object.assign(Comp, {
-        [$DEVCOMP]: true
-      });
       return Comp(props);
     }) : "", {
       sync: true
     });
   };
-  wrap.preload = () => p || ((p = fn()).then(mod => comp = () => mod.default), p);
+  wrap.preload = load;
   wrap.moduleUrl = moduleUrl;
   return wrap;
 }
diff --git a/dist/server.cjs b/dist/server.cjs
index b7652fedf12661b2660550e09352e08c1f716003..e20dc73bbada94f2a669ee3d4ce3360fadba5804 100644
--- a/dist/server.cjs
+++ b/dist/server.cjs
@@ -1303,17 +1303,26 @@ function enableHydration() {}
 function createComponent(Comp, props) {
   return Comp(props || {});
 }
-function lazy(fn, moduleUrl) {
+function lazy(fn, options, moduleUrl) {
+  // rc.0 transform compat: the current bundler pass injects moduleUrl as
+  // the second argument; solid@next moves it third behind the { export }
+  // options bag (56ca6470).
+  if (typeof options === "string") {
+    moduleUrl = options;
+    options = undefined;
+  }
+  const exportName = options && options.export;
   let p;
   let load = () => {
     if (!p) {
-      p = fn();
-      p.then(mod => {
-        p.mod = mod;
-        p.v = mod.default;
+      const cur = p = fn();
+      cur.then(mod => {
+        cur.mod = mod;
+        cur.v = exportName ? mod[exportName] : mod.default;
       }, err => {
-        p.error = err;
-        p.errored = true;
+        cur.error = err;
+        cur.errored = true;
+        if (p === cur) p = undefined;
       });
     }
     return p;
@@ -1323,7 +1332,7 @@ function lazy(fn, moduleUrl) {
     if (!noHydrate && !sharedConfig.context?.resolveAssets) {
       throw new Error(`lazy() called${moduleUrl ? ` with moduleUrl "${moduleUrl}"` : ""} but no asset manifest is set. ` + "Pass a manifest option to renderToStream/renderToString.");
     }
-    load();
+    const cur = load();
     const ctx = sharedConfig.context;
     let assetsPending;
     if (ctx?.registerAsset && ctx.resolveAssets) {
@@ -1370,8 +1379,8 @@ function lazy(fn, moduleUrl) {
       if (moduleUrl) {
         assetsPending = registerLazyAssets(moduleUrl);
       } else if (!noHydrate) {
-        if (p.mod !== undefined) {
-          const id = p.mod?.$$moduleUrl;
+        if (cur.mod !== undefined) {
+          const id = cur.mod?.$$moduleUrl;
           if (typeof id === "string") {
             assetsPending = registerLazyAssets(id);
           } else {
@@ -1379,7 +1388,7 @@ function lazy(fn, moduleUrl) {
           }
         } else {
           const boundary = ctx._currentBoundaryId;
-          assetsPending = p.then(mod => {
+          assetsPending = cur.then(mod => {
             const id = mod?.$$moduleUrl;
             if (typeof id !== "string") {
               console.warn("lazy() used in SSR without a moduleUrl and the loaded module has no " + "$$moduleUrl export, so its client assets cannot be resolved — the " + "component will load late during hydration. This is typically " + "injected by the bundler plugin.");
@@ -1403,10 +1412,10 @@ function lazy(fn, moduleUrl) {
       }
     }
     return createMemo(() => {
-      if (p.errored) throw p.error;
-      if (!p.v) throw new signals.NotReadyError(p);
+      if (cur.errored) throw cur.error;
+      if (!cur.v) throw new signals.NotReadyError(cur);
       if (assetsPending) throw new signals.NotReadyError(assetsPending);
-      return p.v(props);
+      return cur.v(props);
     }, {
       sync: true
     });
diff --git a/dist/server.js b/dist/server.js
index 394d7c0406d4aa0d6caba5faeb0b9d4928c06f1e..a3033f2d21818b4fe30759ae8d420db99cfc116e 100644
--- a/dist/server.js
+++ b/dist/server.js
@@ -1302,17 +1302,26 @@ function enableHydration() {}
 function createComponent(Comp, props) {
   return Comp(props || {});
 }
-function lazy(fn, moduleUrl) {
+function lazy(fn, options, moduleUrl) {
+  // rc.0 transform compat: the current bundler pass injects moduleUrl as
+  // the second argument; solid@next moves it third behind the { export }
+  // options bag (56ca6470).
+  if (typeof options === "string") {
+    moduleUrl = options;
+    options = undefined;
+  }
+  const exportName = options && options.export;
   let p;
   let load = () => {
     if (!p) {
-      p = fn();
-      p.then(mod => {
-        p.mod = mod;
-        p.v = mod.default;
+      const cur = p = fn();
+      cur.then(mod => {
+        cur.mod = mod;
+        cur.v = exportName ? mod[exportName] : mod.default;
       }, err => {
-        p.error = err;
-        p.errored = true;
+        cur.error = err;
+        cur.errored = true;
+        if (p === cur) p = undefined;
       });
     }
     return p;
@@ -1322,7 +1331,7 @@ function lazy(fn, moduleUrl) {
     if (!noHydrate && !sharedConfig.context?.resolveAssets) {
       throw new Error(`lazy() called${moduleUrl ? ` with moduleUrl "${moduleUrl}"` : ""} but no asset manifest is set. ` + "Pass a manifest option to renderToStream/renderToString.");
     }
-    load();
+    const cur = load();
     const ctx = sharedConfig.context;
     let assetsPending;
     if (ctx?.registerAsset && ctx.resolveAssets) {
@@ -1369,8 +1378,8 @@ function lazy(fn, moduleUrl) {
       if (moduleUrl) {
         assetsPending = registerLazyAssets(moduleUrl);
       } else if (!noHydrate) {
-        if (p.mod !== undefined) {
-          const id = p.mod?.$$moduleUrl;
+        if (cur.mod !== undefined) {
+          const id = cur.mod?.$$moduleUrl;
           if (typeof id === "string") {
             assetsPending = registerLazyAssets(id);
           } else {
@@ -1378,7 +1387,7 @@ function lazy(fn, moduleUrl) {
           }
         } else {
           const boundary = ctx._currentBoundaryId;
-          assetsPending = p.then(mod => {
+          assetsPending = cur.then(mod => {
             const id = mod?.$$moduleUrl;
             if (typeof id !== "string") {
               console.warn("lazy() used in SSR without a moduleUrl and the loaded module has no " + "$$moduleUrl export, so its client assets cannot be resolved — the " + "component will load late during hydration. This is typically " + "injected by the bundler plugin.");
@@ -1402,10 +1411,10 @@ function lazy(fn, moduleUrl) {
       }
     }
     return createMemo(() => {
-      if (p.errored) throw p.error;
-      if (!p.v) throw new NotReadyError(p);
+      if (cur.errored) throw cur.error;
+      if (!cur.v) throw new NotReadyError(cur);
       if (assetsPending) throw new NotReadyError(assetsPending);
-      return p.v(props);
+      return cur.v(props);
     }, {
       sync: true
     });
diff --git a/dist/solid.cjs b/dist/solid.cjs
index 2f20f8e0edbc08154e4b2be6040a9f7cbd3ab8f6..67503094cc2bcb73d4574ab82a54cb285d216890 100644
--- a/dist/solid.cjs
+++ b/dist/solid.cjs
@@ -555,11 +555,21 @@ function hydratedCreateRenderEffect(compute, effectFn, options) {
 function hydratedCreateEffect(compute, effectFn, options) {
   return hydratedEffect(signals.createEffect, compute, effectFn, options);
 }
-function lazyHydrationLookup(comp, moduleUrl) {
+function lazyHydrationLookup(comp, moduleUrl, exportName) {
   const o = signals.getOwner();
   const key = o && o.id != null ? signals.peekNextChildId(o) : undefined;
   const cached = key != null ? globalThis._$HY?.modules?.[key] : undefined;
-  if (cached) return () => cached.default;
+  // Hydration resolves the component synchronously from the preloaded module
+  // — its default export, or the call-site `export` option (a literal present
+  // in both bundles, so the sync claim is preserved). A wrapper that selects
+  // an export at runtime inside the import thunk cannot work here: the thunk
+  // hasn't run, and rendering the raw namespace's default would silently
+  // orphan the server DOM (solidjs/solid#3011). Fail loudly in dev instead.
+  if (cached) {
+    const component = exportName ? cached[exportName] : cached.default;
+    if (IS_DEV && typeof component !== "function") throw new Error(`lazy() (hydration id "${key}") preloaded a module whose "${exportName ?? "default"}" ` + "export is not a component. lazy() hydrates synchronously from the preloaded " + "module; select a named export with the { export } option, or re-export the " + "component as the module's default. Wrappers that pick an export at runtime " + "inside the import thunk are not supported.");
+    return () => component;
+  }
   if (!comp && moduleUrl) {
     throw new Error(`lazy() module "${moduleUrl}" (hydration id "${key}") was not preloaded before ` + "hydration. Ensure it is inside a Loading boundary.");
   }
@@ -967,18 +977,33 @@ function Hydration(props) {
 function createComponent(Comp, props) {
   return signals.untrack(() => Comp(props || {}));
 }
-function lazy(fn, moduleUrl) {
+function lazy(fn, options, moduleUrl) {
+  // rc.0 transform compat: the current bundler pass injects moduleUrl as
+  // the second argument; solid@next moves it third behind the { export }
+  // options bag (56ca6470).
+  if (typeof options === "string") {
+    moduleUrl = options;
+    options = undefined;
+  }
+  const exportName = options && options.export;
   let comp;
   let p;
+  const load = () => {
+    if (p) return p;
+    const cur = p = fn();
+    cur.then(mod => {
+      comp = () => (exportName ? mod[exportName] : mod.default);
+    }, () => {
+      if (p === cur) p = undefined;
+    });
+    return cur;
+  };
   const wrap = props => {
-    if (sharedConfig.hydrating) comp = _lazyHydrationLookup(comp, moduleUrl);
+    if (sharedConfig.hydrating) comp = _lazyHydrationLookup(comp, moduleUrl, exportName);
     let local = comp;
     if (!local) {
-      p || (p = fn());
-      p.then(mod => {
-        comp = () => mod.default;
-      });
-      local = signals.createMemo(() => p.then(mod => mod.default));
+      load();
+      local = signals.createMemo(() => load().then(mod => exportName ? mod[exportName] : mod.default));
     }
     let Comp;
     return signals.createMemo(() => (Comp = (comp || local)()) ? signals.untrack(() => {
@@ -987,7 +1012,7 @@ function lazy(fn, moduleUrl) {
       sync: true
     });
   };
-  wrap.preload = () => p || ((p = fn()).then(mod => comp = () => mod.default), p);
+  wrap.preload = load;
   wrap.moduleUrl = moduleUrl;
   return wrap;
 }
diff --git a/dist/solid.js b/dist/solid.js
index 72515e33c15d70e9d075ba7fc39fecbfb6893f66..83e18dab4c7e7ca23d39e013a706f332862630eb 100644
--- a/dist/solid.js
+++ b/dist/solid.js
@@ -554,11 +554,21 @@ function hydratedCreateRenderEffect(compute, effectFn, options) {
 function hydratedCreateEffect(compute, effectFn, options) {
   return hydratedEffect(createEffect$1, compute, effectFn, options);
 }
-function lazyHydrationLookup(comp, moduleUrl) {
+function lazyHydrationLookup(comp, moduleUrl, exportName) {
   const o = getOwner();
   const key = o && o.id != null ? peekNextChildId(o) : undefined;
   const cached = key != null ? globalThis._$HY?.modules?.[key] : undefined;
-  if (cached) return () => cached.default;
+  // Hydration resolves the component synchronously from the preloaded module
+  // — its default export, or the call-site `export` option (a literal present
+  // in both bundles, so the sync claim is preserved). A wrapper that selects
+  // an export at runtime inside the import thunk cannot work here: the thunk
+  // hasn't run, and rendering the raw namespace's default would silently
+  // orphan the server DOM (solidjs/solid#3011). Fail loudly in dev instead.
+  if (cached) {
+    const component = exportName ? cached[exportName] : cached.default;
+    if (IS_DEV && typeof component !== "function") throw new Error(`lazy() (hydration id "${key}") preloaded a module whose "${exportName ?? "default"}" ` + "export is not a component. lazy() hydrates synchronously from the preloaded " + "module; select a named export with the { export } option, or re-export the " + "component as the module's default. Wrappers that pick an export at runtime " + "inside the import thunk are not supported.");
+    return () => component;
+  }
   if (!comp && moduleUrl) {
     throw new Error(`lazy() module "${moduleUrl}" (hydration id "${key}") was not preloaded before ` + "hydration. Ensure it is inside a Loading boundary.");
   }
@@ -966,18 +976,33 @@ function Hydration(props) {
 function createComponent(Comp, props) {
   return untrack(() => Comp(props || {}));
 }
-function lazy(fn, moduleUrl) {
+function lazy(fn, options, moduleUrl) {
+  // rc.0 transform compat: the current bundler pass injects moduleUrl as
+  // the second argument; solid@next moves it third behind the { export }
+  // options bag (56ca6470).
+  if (typeof options === "string") {
+    moduleUrl = options;
+    options = undefined;
+  }
+  const exportName = options && options.export;
   let comp;
   let p;
+  const load = () => {
+    if (p) return p;
+    const cur = p = fn();
+    cur.then(mod => {
+      comp = () => (exportName ? mod[exportName] : mod.default);
+    }, () => {
+      if (p === cur) p = undefined;
+    });
+    return cur;
+  };
   const wrap = props => {
-    if (sharedConfig.hydrating) comp = _lazyHydrationLookup(comp, moduleUrl);
+    if (sharedConfig.hydrating) comp = _lazyHydrationLookup(comp, moduleUrl, exportName);
     let local = comp;
     if (!local) {
-      p || (p = fn());
-      p.then(mod => {
-        comp = () => mod.default;
-      });
-      local = createMemo$1(() => p.then(mod => mod.default));
+      load();
+      local = createMemo$1(() => load().then(mod => exportName ? mod[exportName] : mod.default));
     }
     let Comp;
     return createMemo$1(() => (Comp = (comp || local)()) ? untrack(() => {
@@ -986,7 +1011,7 @@ function lazy(fn, moduleUrl) {
       sync: true
     });
   };
-  wrap.preload = () => p || ((p = fn()).then(mod => comp = () => mod.default), p);
+  wrap.preload = load;
   wrap.moduleUrl = moduleUrl;
   return wrap;
 }
diff --git a/types/client/component.d.ts b/types/client/component.d.ts
index cf1c79e6e60050c400295b247c5e83ae0aff537a..4bd02e0839caec8361901dc9200496951ee90ea7 100644
--- a/types/client/component.d.ts
+++ b/types/client/component.d.ts
@@ -94,9 +94,17 @@ export declare function createComponent<T extends Record<string, any>>(Comp: Com
  * <button onMouseEnter={() => Profile.preload()}>Open profile</button>
  * ```
  */
+export declare function lazy<M extends Record<string, any>, K extends keyof M & string>(fn: () => Promise<M>, options: {
+    export: K;
+}, moduleUrl?: string): M[K] & {
+    preload: () => Promise<M>;
+    moduleUrl?: string;
+};
 export declare function lazy<T extends Component<any>>(fn: () => Promise<{
     default: T;
-}>, moduleUrl?: string): T & {
+}>, options?: {
+    export?: string;
+}, moduleUrl?: string): T & {
     preload: () => Promise<{
         default: T;
     }>;
diff --git a/types/server/component.d.ts b/types/server/component.d.ts
index a10690ecdf71a6886c6b3bb6199c9bb83215ddbb..b0cbcfb337936994bcae01032f4ed598e17c9e92 100644
--- a/types/server/component.d.ts
+++ b/types/server/component.d.ts
@@ -67,9 +67,17 @@ export declare function createComponent<T extends Record<string, any>>(Comp: Com
  * modulepreload hint for the module's chunks — accessing the resolved client
  * URL on the server is treated as a declaration that the client will fetch it.
  */
+export declare function lazy<M extends Record<string, any>, K extends keyof M & string>(fn: () => Promise<M>, options: {
+    export: K;
+}, moduleUrl?: string): M[K] & {
+    preload: () => Promise<M>;
+    moduleUrl?: string;
+};
 export declare function lazy<T extends Component<any>>(fn: () => Promise<{
     default: T;
-}>, moduleUrl?: string): T & {
+}>, options?: {
+    export?: string;
+}, moduleUrl?: string): T & {
     preload: () => Promise<{
         default: T;
     }>;
diff --git a/types-cjs/client/component.d.cts b/types-cjs/client/component.d.cts
index 0b49975ac742e94cd07c5241f1156c2f3af9e7a5..35834d85f14cf0b9231f1616c2d10bc6c4a0a692 100644
--- a/types-cjs/client/component.d.cts
+++ b/types-cjs/client/component.d.cts
@@ -94,9 +94,17 @@ export declare function createComponent<T extends Record<string, any>>(Comp: Com
  * <button onMouseEnter={() => Profile.preload()}>Open profile</button>
  * ```
  */
+export declare function lazy<M extends Record<string, any>, K extends keyof M & string>(fn: () => Promise<M>, options: {
+    export: K;
+}, moduleUrl?: string): M[K] & {
+    preload: () => Promise<M>;
+    moduleUrl?: string;
+};
 export declare function lazy<T extends Component<any>>(fn: () => Promise<{
     default: T;
-}>, moduleUrl?: string): T & {
+}>, options?: {
+    export?: string;
+}, moduleUrl?: string): T & {
     preload: () => Promise<{
         default: T;
     }>;
diff --git a/types-cjs/server/component.d.cts b/types-cjs/server/component.d.cts
index 3ab2390217855cdcf18bc898fbc1a26e2fadbac0..46d8d19a71579ddc62289318c3d5d63573a1c7aa 100644
--- a/types-cjs/server/component.d.cts
+++ b/types-cjs/server/component.d.cts
@@ -67,9 +67,17 @@ export declare function createComponent<T extends Record<string, any>>(Comp: Com
  * modulepreload hint for the module's chunks — accessing the resolved client
  * URL on the server is treated as a declaration that the client will fetch it.
  */
+export declare function lazy<M extends Record<string, any>, K extends keyof M & string>(fn: () => Promise<M>, options: {
+    export: K;
+}, moduleUrl?: string): M[K] & {
+    preload: () => Promise<M>;
+    moduleUrl?: string;
+};
 export declare function lazy<T extends Component<any>>(fn: () => Promise<{
     default: T;
-}>, moduleUrl?: string): T & {
+}>, options?: {
+    export?: string;
+}, moduleUrl?: string): T & {
     preload: () => Promise<{
         default: T;
     }>;
