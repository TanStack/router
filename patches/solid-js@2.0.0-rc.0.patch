diff --git a/dist/dev.cjs b/dist/dev.cjs
index eb15135bbd0f524729b336bdf3b14f5b97101a99..5dfc6231b6155628a0710e8ddf41ed272ad77497 100644
--- a/dist/dev.cjs
+++ b/dist/dev.cjs
@@ -576,7 +576,12 @@ function lazyHydrationLookup(comp, moduleUrl) {
   const o = signals.getOwner();
   const key = o && o.id != null ? signals.peekNextChildId(o) : undefined;
   const cached = key != null ? globalThis._$HY?.modules?.[key] : undefined;
-  if (cached) return () => cached.default;
+  // The preloaded-module shortcut assumes the module's default export is
+  // the component, which only the bundler transform (which injects
+  // moduleUrl) guarantees. Wrappers that select a named export (no
+  // moduleUrl) fall through to their own import so the boundary hydrates
+  // through the async path instead of silently rendering the wrong value.
+  if (cached && moduleUrl) return () => cached.default;
   if (!comp && moduleUrl) {
     throw new Error(`lazy() module "${moduleUrl}" (hydration id "${key}") was not preloaded before ` + "hydration. Ensure it is inside a Loading boundary.");
   }
@@ -987,15 +992,23 @@ function createComponent(Comp, props) {
 function lazy(fn, moduleUrl) {
   let comp;
   let p;
+  const load = () => {
+    if (!p) {
+      const cur = p = fn();
+      cur.then(mod => {
+        comp = () => mod.default;
+      }, () => {
+        if (p === cur) p = undefined;
+      });
+    }
+    return p;
+  };
   const wrap = props => {
     if (sharedConfig.hydrating) comp = _lazyHydrationLookup(comp, moduleUrl);
     let local = comp;
     if (!local) {
-      p || (p = fn());
-      p.then(mod => {
-        comp = () => mod.default;
-      });
-      local = signals.createMemo(() => p.then(mod => mod.default));
+      const cur = load();
+      local = signals.createMemo(() => cur.then(mod => mod.default));
     }
     let Comp;
     return signals.createMemo(() => (Comp = (comp || local)()) ? signals.untrack(() => {
@@ -1007,7 +1020,7 @@ function lazy(fn, moduleUrl) {
       sync: true
     });
   };
-  wrap.preload = () => p || ((p = fn()).then(mod => comp = () => mod.default), p);
+  wrap.preload = load;
   wrap.moduleUrl = moduleUrl;
   return wrap;
 }
diff --git a/dist/dev.js b/dist/dev.js
index e9b692addaef5c1285a9003fe4ff360e91d40d79..a77453f7caec15ea93e527bb23fe0ea00d56cef8 100644
--- a/dist/dev.js
+++ b/dist/dev.js
@@ -575,7 +575,12 @@ function lazyHydrationLookup(comp, moduleUrl) {
   const o = getOwner();
   const key = o && o.id != null ? peekNextChildId(o) : undefined;
   const cached = key != null ? globalThis._$HY?.modules?.[key] : undefined;
-  if (cached) return () => cached.default;
+  // The preloaded-module shortcut assumes the module's default export is
+  // the component, which only the bundler transform (which injects
+  // moduleUrl) guarantees. Wrappers that select a named export (no
+  // moduleUrl) fall through to their own import so the boundary hydrates
+  // through the async path instead of silently rendering the wrong value.
+  if (cached && moduleUrl) return () => cached.default;
   if (!comp && moduleUrl) {
     throw new Error(`lazy() module "${moduleUrl}" (hydration id "${key}") was not preloaded before ` + "hydration. Ensure it is inside a Loading boundary.");
   }
@@ -986,15 +991,23 @@ function createComponent(Comp, props) {
 function lazy(fn, moduleUrl) {
   let comp;
   let p;
+  const load = () => {
+    if (!p) {
+      const cur = p = fn();
+      cur.then(mod => {
+        comp = () => mod.default;
+      }, () => {
+        if (p === cur) p = undefined;
+      });
+    }
+    return p;
+  };
   const wrap = props => {
     if (sharedConfig.hydrating) comp = _lazyHydrationLookup(comp, moduleUrl);
     let local = comp;
     if (!local) {
-      p || (p = fn());
-      p.then(mod => {
-        comp = () => mod.default;
-      });
-      local = createMemo$1(() => p.then(mod => mod.default));
+      const cur = load();
+      local = createMemo$1(() => cur.then(mod => mod.default));
     }
     let Comp;
     return createMemo$1(() => (Comp = (comp || local)()) ? untrack(() => {
@@ -1006,7 +1019,7 @@ function lazy(fn, moduleUrl) {
       sync: true
     });
   };
-  wrap.preload = () => p || ((p = fn()).then(mod => comp = () => mod.default), p);
+  wrap.preload = load;
   wrap.moduleUrl = moduleUrl;
   return wrap;
 }
diff --git a/dist/server.cjs b/dist/server.cjs
index b7652fedf12661b2660550e09352e08c1f716003..95801d7b76c5a368e859f62603cb5b36f2a34e75 100644
--- a/dist/server.cjs
+++ b/dist/server.cjs
@@ -1307,13 +1307,14 @@ function lazy(fn, moduleUrl) {
   let p;
   let load = () => {
     if (!p) {
-      p = fn();
-      p.then(mod => {
-        p.mod = mod;
-        p.v = mod.default;
+      const cur = p = fn();
+      cur.then(mod => {
+        cur.mod = mod;
+        cur.v = mod.default;
       }, err => {
-        p.error = err;
-        p.errored = true;
+        cur.error = err;
+        cur.errored = true;
+        if (p === cur) p = undefined;
       });
     }
     return p;
@@ -1323,7 +1324,7 @@ function lazy(fn, moduleUrl) {
     if (!noHydrate && !sharedConfig.context?.resolveAssets) {
       throw new Error(`lazy() called${moduleUrl ? ` with moduleUrl "${moduleUrl}"` : ""} but no asset manifest is set. ` + "Pass a manifest option to renderToStream/renderToString.");
     }
-    load();
+    const cur = load();
     const ctx = sharedConfig.context;
     let assetsPending;
     if (ctx?.registerAsset && ctx.resolveAssets) {
@@ -1370,8 +1371,8 @@ function lazy(fn, moduleUrl) {
       if (moduleUrl) {
         assetsPending = registerLazyAssets(moduleUrl);
       } else if (!noHydrate) {
-        if (p.mod !== undefined) {
-          const id = p.mod?.$$moduleUrl;
+        if (cur.mod !== undefined) {
+          const id = cur.mod?.$$moduleUrl;
           if (typeof id === "string") {
             assetsPending = registerLazyAssets(id);
           } else {
@@ -1379,7 +1380,7 @@ function lazy(fn, moduleUrl) {
           }
         } else {
           const boundary = ctx._currentBoundaryId;
-          assetsPending = p.then(mod => {
+          assetsPending = cur.then(mod => {
             const id = mod?.$$moduleUrl;
             if (typeof id !== "string") {
               console.warn("lazy() used in SSR without a moduleUrl and the loaded module has no " + "$$moduleUrl export, so its client assets cannot be resolved — the " + "component will load late during hydration. This is typically " + "injected by the bundler plugin.");
@@ -1403,10 +1404,10 @@ function lazy(fn, moduleUrl) {
       }
     }
     return createMemo(() => {
-      if (p.errored) throw p.error;
-      if (!p.v) throw new signals.NotReadyError(p);
+      if (cur.errored) throw cur.error;
+      if (!cur.v) throw new signals.NotReadyError(cur);
       if (assetsPending) throw new signals.NotReadyError(assetsPending);
-      return p.v(props);
+      return cur.v(props);
     }, {
       sync: true
     });
diff --git a/dist/server.js b/dist/server.js
index 394d7c0406d4aa0d6caba5faeb0b9d4928c06f1e..f75a357bcee91d3305c3e67127c8e7d199708c68 100644
--- a/dist/server.js
+++ b/dist/server.js
@@ -1306,13 +1306,14 @@ function lazy(fn, moduleUrl) {
   let p;
   let load = () => {
     if (!p) {
-      p = fn();
-      p.then(mod => {
-        p.mod = mod;
-        p.v = mod.default;
+      const cur = p = fn();
+      cur.then(mod => {
+        cur.mod = mod;
+        cur.v = mod.default;
       }, err => {
-        p.error = err;
-        p.errored = true;
+        cur.error = err;
+        cur.errored = true;
+        if (p === cur) p = undefined;
       });
     }
     return p;
@@ -1322,7 +1323,7 @@ function lazy(fn, moduleUrl) {
     if (!noHydrate && !sharedConfig.context?.resolveAssets) {
       throw new Error(`lazy() called${moduleUrl ? ` with moduleUrl "${moduleUrl}"` : ""} but no asset manifest is set. ` + "Pass a manifest option to renderToStream/renderToString.");
     }
-    load();
+    const cur = load();
     const ctx = sharedConfig.context;
     let assetsPending;
     if (ctx?.registerAsset && ctx.resolveAssets) {
@@ -1369,8 +1370,8 @@ function lazy(fn, moduleUrl) {
       if (moduleUrl) {
         assetsPending = registerLazyAssets(moduleUrl);
       } else if (!noHydrate) {
-        if (p.mod !== undefined) {
-          const id = p.mod?.$$moduleUrl;
+        if (cur.mod !== undefined) {
+          const id = cur.mod?.$$moduleUrl;
           if (typeof id === "string") {
             assetsPending = registerLazyAssets(id);
           } else {
@@ -1378,7 +1379,7 @@ function lazy(fn, moduleUrl) {
           }
         } else {
           const boundary = ctx._currentBoundaryId;
-          assetsPending = p.then(mod => {
+          assetsPending = cur.then(mod => {
             const id = mod?.$$moduleUrl;
             if (typeof id !== "string") {
               console.warn("lazy() used in SSR without a moduleUrl and the loaded module has no " + "$$moduleUrl export, so its client assets cannot be resolved — the " + "component will load late during hydration. This is typically " + "injected by the bundler plugin.");
@@ -1402,10 +1403,10 @@ function lazy(fn, moduleUrl) {
       }
     }
     return createMemo(() => {
-      if (p.errored) throw p.error;
-      if (!p.v) throw new NotReadyError(p);
+      if (cur.errored) throw cur.error;
+      if (!cur.v) throw new NotReadyError(cur);
       if (assetsPending) throw new NotReadyError(assetsPending);
-      return p.v(props);
+      return cur.v(props);
     }, {
       sync: true
     });
diff --git a/dist/solid.cjs b/dist/solid.cjs
index 2f20f8e0edbc08154e4b2be6040a9f7cbd3ab8f6..8ce322189ae805c01b4646e509acfc8c529fe9ed 100644
--- a/dist/solid.cjs
+++ b/dist/solid.cjs
@@ -559,7 +559,12 @@ function lazyHydrationLookup(comp, moduleUrl) {
   const o = signals.getOwner();
   const key = o && o.id != null ? signals.peekNextChildId(o) : undefined;
   const cached = key != null ? globalThis._$HY?.modules?.[key] : undefined;
-  if (cached) return () => cached.default;
+  // The preloaded-module shortcut assumes the module's default export is
+  // the component, which only the bundler transform (which injects
+  // moduleUrl) guarantees. Wrappers that select a named export (no
+  // moduleUrl) fall through to their own import so the boundary hydrates
+  // through the async path instead of silently rendering the wrong value.
+  if (cached && moduleUrl) return () => cached.default;
   if (!comp && moduleUrl) {
     throw new Error(`lazy() module "${moduleUrl}" (hydration id "${key}") was not preloaded before ` + "hydration. Ensure it is inside a Loading boundary.");
   }
@@ -970,15 +975,23 @@ function createComponent(Comp, props) {
 function lazy(fn, moduleUrl) {
   let comp;
   let p;
+  const load = () => {
+    if (!p) {
+      const cur = p = fn();
+      cur.then(mod => {
+        comp = () => mod.default;
+      }, () => {
+        if (p === cur) p = undefined;
+      });
+    }
+    return p;
+  };
   const wrap = props => {
     if (sharedConfig.hydrating) comp = _lazyHydrationLookup(comp, moduleUrl);
     let local = comp;
     if (!local) {
-      p || (p = fn());
-      p.then(mod => {
-        comp = () => mod.default;
-      });
-      local = signals.createMemo(() => p.then(mod => mod.default));
+      const cur = load();
+      local = signals.createMemo(() => cur.then(mod => mod.default));
     }
     let Comp;
     return signals.createMemo(() => (Comp = (comp || local)()) ? signals.untrack(() => {
@@ -987,7 +1000,7 @@ function lazy(fn, moduleUrl) {
       sync: true
     });
   };
-  wrap.preload = () => p || ((p = fn()).then(mod => comp = () => mod.default), p);
+  wrap.preload = load;
   wrap.moduleUrl = moduleUrl;
   return wrap;
 }
diff --git a/dist/solid.js b/dist/solid.js
index 72515e33c15d70e9d075ba7fc39fecbfb6893f66..c73bc2a4fc14c0a196d66e3abe67c710d4a5b8d3 100644
--- a/dist/solid.js
+++ b/dist/solid.js
@@ -558,7 +558,12 @@ function lazyHydrationLookup(comp, moduleUrl) {
   const o = getOwner();
   const key = o && o.id != null ? peekNextChildId(o) : undefined;
   const cached = key != null ? globalThis._$HY?.modules?.[key] : undefined;
-  if (cached) return () => cached.default;
+  // The preloaded-module shortcut assumes the module's default export is
+  // the component, which only the bundler transform (which injects
+  // moduleUrl) guarantees. Wrappers that select a named export (no
+  // moduleUrl) fall through to their own import so the boundary hydrates
+  // through the async path instead of silently rendering the wrong value.
+  if (cached && moduleUrl) return () => cached.default;
   if (!comp && moduleUrl) {
     throw new Error(`lazy() module "${moduleUrl}" (hydration id "${key}") was not preloaded before ` + "hydration. Ensure it is inside a Loading boundary.");
   }
@@ -969,15 +974,23 @@ function createComponent(Comp, props) {
 function lazy(fn, moduleUrl) {
   let comp;
   let p;
+  const load = () => {
+    if (!p) {
+      const cur = p = fn();
+      cur.then(mod => {
+        comp = () => mod.default;
+      }, () => {
+        if (p === cur) p = undefined;
+      });
+    }
+    return p;
+  };
   const wrap = props => {
     if (sharedConfig.hydrating) comp = _lazyHydrationLookup(comp, moduleUrl);
     let local = comp;
     if (!local) {
-      p || (p = fn());
-      p.then(mod => {
-        comp = () => mod.default;
-      });
-      local = createMemo$1(() => p.then(mod => mod.default));
+      const cur = load();
+      local = createMemo$1(() => cur.then(mod => mod.default));
     }
     let Comp;
     return createMemo$1(() => (Comp = (comp || local)()) ? untrack(() => {
@@ -986,7 +999,7 @@ function lazy(fn, moduleUrl) {
       sync: true
     });
   };
-  wrap.preload = () => p || ((p = fn()).then(mod => comp = () => mod.default), p);
+  wrap.preload = load;
   wrap.moduleUrl = moduleUrl;
   return wrap;
 }
