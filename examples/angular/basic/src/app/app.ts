import { Component } from '@angular/core'
import { RouterRoot } from '@tanstack/angular-router'

@Component({
  selector: 'app-root',
  imports: [RouterRoot],
  template: ` <RouterRoot /> `,
})
export class AppComponent {}
